name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  lint-python:
    name: Ruff (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - run: pip install ruff==0.14.5
      - run: ruff check custom_components tests

  frontend:
    name: Build card bundle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - name: Verify bundle is fresh
        run: |
          if [ -n "$(git status --porcelain custom_components/openmap/frontend/dist)" ]; then
            echo "::error::dist/openmap-card.js is outdated — run 'npm run build' and commit the result"
            git diff --stat
            exit 1
          fi

  tests:
    name: pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest-homeassistant-custom-component
      - name: Run tests
        run: python -m pytest tests/ -v --timeout=60 --cov=custom_components/openmap --cov-report=term
